
com.addthis.hydra {
  job.spawn.Spawn {
    # disable the git-based job store and message queue for unit tests
    jobStore  = null

    # disable the message queue for unit tests
    queueType = null
  }
  job.minion.Minion {
    # disable the message queue for unit tests
    queueType = null
  }
}
