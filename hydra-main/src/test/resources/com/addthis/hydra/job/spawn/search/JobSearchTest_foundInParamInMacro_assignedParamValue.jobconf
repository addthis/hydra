job searcher unit test job

this job does not have the search string directly in its job config but it references a macro
%{JobSearchTest_foundInParamInMacro_assignedParamValue.macro}% with a job parameter that does,
so it should be a match